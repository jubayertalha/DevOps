apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ingress.nameB }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.rabbitmq.host }}
      http:
        paths:
          - pathType: Prefix
            backend:
              service:
                name: {{ .Values.rabbitmq.name }}
                port:
                  number: {{ .Values.rabbitmq.port }}
            path: {{ .Values.rabbitmq.path }}