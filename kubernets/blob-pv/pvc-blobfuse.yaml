apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-blob-fuse
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-blob-fuse
  storageClassName: azureblob-fuse-premium