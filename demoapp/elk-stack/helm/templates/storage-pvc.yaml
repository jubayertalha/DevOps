kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-blob-nfs
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-blob-nfs
  storageClassName: azureblob-nfs-premium