apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ignite.fullname" . }}-test-connection"
  labels:
    {{- include "ignite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ignite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
